# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: mpozuelo-index_BC-latest
channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  ##conda forge packages
  - conda-forge::python=3.7
  - conda-forge::pigz=2.3.4

  - pandas=1.2.1
